steps:
  # Build the Docker image with extended timeout
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'gcr.io/quantumsentinel-nexus-9957/quantum-sentinel-reconnaissance:latest',
      '-f', 'Dockerfile.full',
      '.'
    ]
    timeout: 3600s

  # Push the image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'push',
      'gcr.io/quantumsentinel-nexus-9957/quantum-sentinel-reconnaissance:latest'
    ]

  # Deploy to Cloud Run with optimized configuration for OSINT workloads
  - name: 'gcr.io/cloud-builders/gcloud'
    args: [
      'run', 'deploy', 'quantum-sentinel-reconnaissance',
      '--image=gcr.io/quantumsentinel-nexus-9957/quantum-sentinel-reconnaissance:latest',
      '--platform=managed',
      '--region=us-central1',
      '--allow-unauthenticated',
      '--port=8000',
      '--memory=8Gi',
      '--cpu=4',
      '--timeout=3600',
      '--max-instances=5',
      '--concurrency=10'
    ]

# Set timeout for OSINT tool builds
timeout: 7200s