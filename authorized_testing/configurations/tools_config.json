{
  "frida_scripts": {
    "ssl_bypass": {
      "description": "SSL certificate pinning bypass for authorized testing",
      "usage": "frida -U -f [AUTHORIZED_APP_BUNDLE_ID] -l ssl_bypass.js --no-pause",
      "authorization_required": true,
      "notes": "Only use on applications you own or have written permission to test"
    },
    "runtime_analysis": {
      "description": "Runtime analysis and method hooking",
      "usage": "frida -U [AUTHORIZED_APP_BUNDLE_ID] -l runtime_hooks.js",
      "authorization_required": true,
      "notes": "Capture method calls and analyze security controls"
    }
  },
  "burp_suite_setup": {
    "proxy_configuration": "Configure mobile device to use Burp proxy",
    "ssl_certificate": "Install Burp CA certificate on test device",
    "scope_settings": "Limit scope to authorized testing targets only"
  }
}